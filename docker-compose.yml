version: '3.8'
services:
  dev:
    build: .
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./package.json:/app/package.json
    ports:
      - "3000:3000"
      - "5173:5173"  # Vite default port
    environment:
      - NODE_ENV=development